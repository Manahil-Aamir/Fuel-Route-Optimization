# Core
django>=6.0
djangorestframework>=3.15
 
# Data processing
pandas>=2.0
numpy>=1.26
 
# HTTP
requests>=2.31s
 
# Cache backend (optional — only needed if using Redis)
django-redis>=5.4
 
# Production server (replace Django's dev server)
gunicorn>=21.2